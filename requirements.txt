# Web framework
flask>=3.0,<4.0

# JamAI SDK (pick any released version >=0.4.0, <2.0)
jamaibase
# Production WSGI server
gunicorn>=20.0,<24.0

# HTTP client
requests>=2.31,<3.0

# Optional: CORS support for local dev
flask-cors>=4.0,<6.0
